apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: menus.widgets.templates.krateo.io
spec:
  group: widgets.templates.krateo.io
  names:
    categories:
      - widgets
      - krateo
    kind: Menu
    listKind: MenuList
    plural: menus
    singular: menu
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                apiRef:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                    - name
                    - namespace
                  type: object
                resourcesRefs:
                  properties:
                    items:
                      items:
                        properties:
                          apiVersion:
                            type: string
                          id:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                          payload:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          resource:
                            type: string
                          slice:
                            properties:
                              continue:
                                type: boolean
                              offset:
                                format: int32
                                type: integer
                              page:
                                format: int32
                                type: integer
                              perPage:
                                format: int32
                                type: integer
                            required:
                              - page
                              - perPage
                            type: object
                          verb:
                            enum:
                              - POST
                              - PUT
                              - PATCH
                              - DELETE
                              - GET
                            type: string
                        required:
                          - id
                        type: object
                      type: array
                    slice:
                      properties:
                        continue:
                          type: boolean
                        offset:
                          format: int32
                          type: integer
                        page:
                          format: int32
                          type: integer
                        perPage:
                          format: int32
                          type: integer
                      required:
                        - page
                        - perPage
                      type: object
                  required:
                    - items
                  type: object
                resourcesRefsTemplate:
                  items:
                    properties:
                      iterator:
                        type: string
                      template:
                        properties:
                          apiVersion:
                            type: string
                          id:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                          payload:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          resource:
                            type: string
                          verb:
                            enum:
                              - POST
                              - PUT
                              - PATCH
                              - DELETE
                              - GET
                            type: string
                        type: object
                    type: object
                  type: array
                widgetData:
                  properties:
                    allowedResources:
                      description: the list of resources that are allowed to be children of this widget or referenced by it
                      items:
                        enum:
                          - navmenuitems
                          - pages
                        type: string
                      type: array
                    items:
                      description: navigation entries (inline nav data); each references its content widget by resourceRefId or resolves via the path → flexes/page-<slug> convention. A label-less item registers a route with no sidebar entry.
                      items:
                        properties:
                          icon:
                            description: FontAwesome icon name shown beside the label (e.g. 'fa-inbox')
                            type: string
                          label:
                            description: menu entry label; omit for a route-only (hidden) item
                            type: string
                          order:
                            default: 100
                            description: sort weight for the entry
                            format: int32
                            type: integer
                          page:
                            description: convention page-slug override → content is flexes/page-<slug>; set this for templated paths to avoid list-vs-detail collisions.
                            type: string
                          path:
                            description: route path; '{param}' segments become :param and reach the content widget via ?extras. A label-less item registers a route with NO sidebar entry (hidden — e.g. detail/create/search).
                            type: string
                          resourceRefId:
                            description: id of the content widget (resolved via resourcesRefs, RBAC-aware). Optional — omit to use the path → flexes/page-<slug> convention.
                            type: string
                          type:
                            description: set to 'divider' to render a visual separator at this order position; no path or label needed
                            enum:
                              - divider
                            type: string
                        type: object
                      type: array
                    mode:
                      description: antd Menu mode (default inline)
                      enum:
                        - vertical
                        - horizontal
                        - inline
                      type: string
                    theme:
                      description: antd Menu theme
                      enum:
                        - light
                        - dark
                      type: string
                  required:
                    - allowedResources
                    - items
                  type: object
                widgetDataTemplate:
                  items:
                    properties:
                      expression:
                        type: string
                      forPath:
                        type: string
                    type: object
                  type: array
                identityContext:
                  items:
                    enum:
                      - username
                      - groups
                    type: string
                  type: array
                keyExtras:
                  description: 'F6 (snowplow#130): the request-extras keys (route params / URL query folded into ?extras=) whose values this widget''s resolution depends on. Only declared keys partition the widget cache cell. Absent/empty = the widget does not vary by any request extra (chrome/layout default).'
                  items:
                    type: string
                  type: array
                freshness:
                  description: 'Opt-in staleness indicator: when true the widget shows the stale/refreshing dot overlay (exception states only). Absent/false = no badge ever (the default; healthy widgets stay unmarked).'
                  type: boolean
              required:
                - widgetData
              type: object
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
          type: object
      served: true
      storage: true
      subresources:
        status: {}
