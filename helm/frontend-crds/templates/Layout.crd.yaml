apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: layouts.widgets.templates.krateo.io
spec:
  group: widgets.templates.krateo.io
  names:
    categories:
      - widgets
      - krateo
    kind: Layout
    listKind: LayoutList
    plural: layouts
    singular: layout
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .metadata.creationTimestamp
          name: AGE
          type: date
      name: v1beta1
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              properties:
                apiRef:
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                    - name
                    - namespace
                  type: object
                resourcesRefs:
                  properties:
                    items:
                      items:
                        properties:
                          apiVersion:
                            type: string
                          id:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                          payload:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          resource:
                            type: string
                          slice:
                            properties:
                              continue:
                                type: boolean
                              offset:
                                format: int32
                                type: integer
                              page:
                                format: int32
                                type: integer
                              perPage:
                                format: int32
                                type: integer
                            required:
                              - page
                              - perPage
                            type: object
                          verb:
                            enum:
                              - POST
                              - PUT
                              - PATCH
                              - DELETE
                              - GET
                            type: string
                        required:
                          - id
                        type: object
                      type: array
                    slice:
                      properties:
                        continue:
                          type: boolean
                        offset:
                          format: int32
                          type: integer
                        page:
                          format: int32
                          type: integer
                        perPage:
                          format: int32
                          type: integer
                      required:
                        - page
                        - perPage
                      type: object
                  required:
                    - items
                  type: object
                resourcesRefsTemplate:
                  items:
                    properties:
                      iterator:
                        type: string
                      template:
                        properties:
                          apiVersion:
                            type: string
                          id:
                            type: string
                          name:
                            type: string
                          namespace:
                            type: string
                          payload:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          resource:
                            type: string
                          verb:
                            enum:
                              - POST
                              - PUT
                              - PATCH
                              - DELETE
                              - GET
                            type: string
                        type: object
                    type: object
                  type: array
                widgetData:
                  properties:
                    content:
                      description: resourceRefId of the widget rendered in antd Layout.Content
                      type: string
                    effects:
                      description: 'resourceRefIds of side-effect widgets (e.g. Theme) rendered invisibly: they set global state (CSS custom properties, etc.) and produce no UI. Use for app-wide concerns mounted once on the shell.'
                      items:
                        type: string
                      type: array
                    footer:
                      description: resourceRefId of the widget rendered in antd Layout.Footer
                      type: string
                    hasSider:
                      description: antd Layout `hasSider` — declares a Sider child so the flex direction is correct on first paint
                      type: boolean
                    header:
                      description: resourceRefId of the widget rendered in antd Layout.Header
                      type: string
                    sider:
                      description: antd Layout.Sider region
                      properties:
                        breakpoint:
                          description: antd Sider responsive `breakpoint`; auto-collapses below it
                          enum:
                            - xs
                            - sm
                            - md
                            - lg
                            - xl
                            - xxl
                          type: string
                        collapsedWidth:
                          description: antd Sider `collapsedWidth` in px
                          format: int32
                          type: integer
                        collapsible:
                          description: antd Sider `collapsible` (renders a collapse trigger)
                          type: boolean
                        defaultCollapsed:
                          description: antd Sider `defaultCollapsed`
                          type: boolean
                        resourceRefId:
                          description: resourceRefId of the widget rendered inside the Sider
                          type: string
                        reverseArrow:
                          description: antd Sider `reverseArrow`
                          type: boolean
                        theme:
                          description: antd Sider `theme`
                          enum:
                            - light
                            - dark
                          type: string
                        width:
                          description: antd Sider `width` in px
                          format: int32
                          type: integer
                      type: object
                  type: object
                widgetDataTemplate:
                  items:
                    properties:
                      expression:
                        type: string
                      forPath:
                        type: string
                    type: object
                  type: array
                identityContext:
                  items:
                    enum:
                      - username
                      - groups
                    type: string
                  type: array
                keyExtras:
                  description: 'F6 (snowplow#130): the request-extras keys (route params / URL query folded into ?extras=) whose values this widget''s resolution depends on. Only declared keys partition the widget cache cell. Absent/empty = the widget does not vary by any request extra (chrome/layout default).'
                  items:
                    type: string
                  type: array
                freshness:
                  description: 'Opt-in staleness indicator: when true the widget shows the stale/refreshing dot overlay (exception states only). Absent/false = no badge ever (the default; healthy widgets stay unmarked).'
                  type: boolean
              required:
                - widgetData
              type: object
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
          type: object
      served: true
      storage: true
      subresources:
        status: {}
