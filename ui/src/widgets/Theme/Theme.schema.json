{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "default": "v1beta1",
      "description": "Widget API version."
    },
    "kind": {
      "type": "string",
      "default": "Theme",
      "description": "Theme applies tenant brand overrides app-wide (Brand v2, issue #49 \u00a77). It renders no visible UI \u2014 a side-effect widget that sets CSS custom properties on :root. Mount one globally (e.g. in the app-shell)."
    },
    "spec": {
      "type": "object",
      "properties": {
        "widgetData": {
          "type": "object",
          "additionalProperties": false,
          "description": "Tenant Theme overrides (Brand v2, issue #49 \u00a77). Every field is optional \u2014 a Theme overrides any SUBSET. Tier-2 tokens (status/agent/focus ring/chart palette) are intentionally NOT here: they are locked and cannot be overridden by a tenant.",
          "properties": {
            "mode": {
              "type": "string",
              "enum": [
                "dark",
                "light"
              ],
              "description": "when set, PIN the color mode (overrides the user's light/dark toggle preference for this tenant)"
            },
            "logo": {
              "type": "object",
              "additionalProperties": false,
              "description": "tenant brand logo (used on the login panel; sider brand is chart-driven)",
              "properties": {
                "url": {
                  "type": "string",
                  "description": "logo image URL"
                },
                "alt": {
                  "type": "string",
                  "description": "logo alt text"
                }
              }
            },
            "token": {
              "type": "object",
              "additionalProperties": false,
              "description": "overridable Tier-1 design tokens. Applied as CSS custom properties (--krateo-* / legacy --*-color) at runtime, so CSS-module and index.css styling re-tint. Only these named tokens are overridable.",
              "properties": {
                "colorPrimary": {
                  "type": "string",
                  "description": "brand/interaction colour \u2192 --primary-color + --krateo-color-action-primary"
                },
                "colorBgLayout": {
                  "type": "string",
                  "description": "page background \u2192 --background-color + --krateo-color-background-base"
                },
                "colorBgContainer": {
                  "type": "string",
                  "description": "card/panel surface \u2192 --panelbg-color + --krateo-color-background-surface"
                },
                "colorBorder": {
                  "type": "string",
                  "description": "hairline border \u2192 --border-color + --krateo-color-border-subtle"
                },
                "colorText": {
                  "type": "string",
                  "description": "default text \u2192 --text-color + --krateo-color-text-default"
                },
                "fontFamily": {
                  "type": "string",
                  "description": "UI font stack \u2192 --font-family + --krateo-font-ui"
                },
                "fontSize": {
                  "type": "integer",
                  "description": "base font size in px"
                }
              }
            },
            "custom": {
              "type": "object",
              "additionalProperties": false,
              "description": "non-token brand chrome overrides (sidebar + nav)",
              "properties": {
                "sidebar": {
                  "type": "object",
                  "additionalProperties": false,
                  "description": "sidebar rail gradient stops (\u2192 --krateo-nav-gradient-* / --menubgstart/end-color)",
                  "properties": {
                    "bgGradientStart": {
                      "type": "string",
                      "description": "Start colour of the sidebar background gradient."
                    },
                    "bgGradientEnd": {
                      "type": "string",
                      "description": "End colour of the sidebar background gradient."
                    }
                  }
                },
                "menu": {
                  "type": "object",
                  "additionalProperties": false,
                  "description": "sidebar nav-item colours (\u2192 --menuitem-color / --menuitembg-color / --krateo-nav-item*)",
                  "properties": {
                    "itemColor": {
                      "type": "string",
                      "description": "Text colour of an idle menu item."
                    },
                    "itemHoverColor": {
                      "type": "string",
                      "description": "Text colour of a menu item under the pointer."
                    },
                    "itemSelectedBg": {
                      "type": "string",
                      "description": "Background of the selected menu item."
                    },
                    "itemSelectedColor": {
                      "type": "string",
                      "description": "Text colour of the selected menu item."
                    }
                  }
                }
              }
            }
          }
        },
        "apiRef": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Name of the RESTAction resource to call."
            },
            "namespace": {
              "type": "string",
              "description": "Namespace of the RESTAction resource."
            }
          },
          "required": [
            "name",
            "namespace"
          ],
          "additionalProperties": false,
          "description": "Binds this widget to a RESTAction whose response feeds the widgetDataTemplate and resourcesRefsTemplate expressions. Omit it for a widget whose data is entirely static."
        },
        "widgetDataTemplate": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "forPath": {
                "type": "string",
                "description": "Dot-path inside widgetData whose value this expression replaces, for example `items[0].title`."
              },
              "expression": {
                "type": "string",
                "description": "A jq expression evaluated server-side against the apiRef response. Its result replaces the value at forPath."
              }
            },
            "additionalProperties": false
          },
          "description": "Per-field overrides evaluated against the apiRef response, so one authored widget can render live cluster data. Each entry replaces one value inside widgetData."
        },
        "resourcesRefs": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "allowed": {
                    "type": "boolean",
                    "description": "Set server-side: whether the caller may perform this verb. False entries are stripped before the widget renders and are never authored by hand."
                  },
                  "apiVersion": {
                    "type": "string",
                    "description": "Group and version of the referenced resource, for example `widgets.templates.krateo.io/v1beta1`."
                  },
                  "id": {
                    "type": "string",
                    "description": "The author-chosen handle this widget uses to reference the resource, matching a resourceRefId in widgetData."
                  },
                  "name": {
                    "type": "string",
                    "description": "Name of the referenced resource."
                  },
                  "namespace": {
                    "type": "string",
                    "description": "Namespace of the referenced resource."
                  },
                  "payload": {
                    "type": "object",
                    "description": "Body sent with a mutating verb. Ignored for a GET."
                  },
                  "resource": {
                    "type": "string",
                    "description": "Plural resource name, for example `paragraphs`."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "POST",
                      "PUT",
                      "PATCH",
                      "DELETE",
                      "GET"
                    ],
                    "description": "HTTP verb used to resolve the reference. GET for a read; a mutating verb makes this a write the widget can dispatch."
                  },
                  "slice": {
                    "type": "object",
                    "properties": {
                      "offset": {
                        "type": "integer",
                        "description": "Number of items to skip before the returned page."
                      },
                      "page": {
                        "type": "integer",
                        "description": "One-based page number to return."
                      },
                      "perPage": {
                        "type": "integer",
                        "description": "Maximum items in the returned page."
                      },
                      "continue": {
                        "type": "boolean",
                        "description": "Opaque continuation token from a previous response, used to fetch the next page."
                      }
                    },
                    "required": [
                      "page",
                      "perPage"
                    ],
                    "description": "Server-side paging for this reference: the widget receives one page rather than the whole list."
                  }
                },
                "required": [
                  "id"
                ]
              },
              "description": "One referenced resource. The widget addresses it by its `id`, never by name."
            }
          },
          "required": [
            "items"
          ],
          "description": "The resources this widget references by id. Each entry is resolved server-side and its endpoint handed to the widget; entries the caller may not read are removed before the widget renders, so a denied reference reads as absence rather than as an error."
        },
        "resourcesRefsTemplate": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "iterator": {
                "type": "string",
                "description": "A jq expression selecting the array in the apiRef response to iterate over."
              },
              "template": {
                "type": "object",
                "properties": {
                  "apiVersion": {
                    "type": "string",
                    "description": "Group and version of the referenced resource, for example `widgets.templates.krateo.io/v1beta1`."
                  },
                  "id": {
                    "type": "string",
                    "description": "The author-chosen handle this widget uses to reference the resource, matching a resourceRefId in widgetData."
                  },
                  "name": {
                    "type": "string",
                    "description": "Name of the referenced resource."
                  },
                  "namespace": {
                    "type": "string",
                    "description": "Namespace of the referenced resource."
                  },
                  "payload": {
                    "type": "object",
                    "description": "Body sent with a mutating verb. Ignored for a GET."
                  },
                  "resource": {
                    "type": "string",
                    "description": "Plural resource name, for example `paragraphs`."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "POST",
                      "PUT",
                      "PATCH",
                      "DELETE",
                      "GET"
                    ],
                    "description": "HTTP verb used to resolve the reference. GET for a read; a mutating verb makes this a write the widget can dispatch."
                  }
                },
                "additionalProperties": false,
                "description": "The resourcesRefs entry emitted for each iterated element; its fields may interpolate that element."
              }
            },
            "additionalProperties": false
          },
          "description": "Generates resourcesRefs entries from the apiRef response \u2014 one per element the iterator yields \u2014 so a widget can reference a list whose length it does not know when authored."
        }
      },
      "required": [
        "widgetData"
      ],
      "description": "The widget's contract: its own data, an optional binding to a RESTAction, the resources it references, and any templated overrides."
    }
  },
  "required": [
    "kind",
    "spec",
    "version"
  ]
}
