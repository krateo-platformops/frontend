{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "string",
      "default": "v1beta1",
      "description": "Widget API version."
    },
    "kind": {
      "default": "Table",
      "description": "Table displays structured data with customizable columns and pagination",
      "type": "string"
    },
    "spec": {
      "type": "object",
      "properties": {
        "widgetData": {
          "type": "object",
          "required": [
            "allowedResources",
            "columns"
          ],
          "properties": {
            "allowedResources": {
              "description": "the list of resources that are allowed to be children of this widget or referenced by it",
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "barcharts",
                  "buttons",
                  "buttongroups",
                  "filters",
                  "flowcharts",
                  "linecharts",
                  "markdowns",
                  "paragraphs",
                  "piecharts",
                  "rangepickers",
                  "yamlviewers"
                ]
              }
            },
            "columns": {
              "description": "configuration of the table's columns",
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "color": {
                    "description": "the color of the value (or the icon) to be represented. These are KRATEO PALETTE names resolved through the shared palette \u2014 not antd presets. The same name on a Button takes antd's vocabulary instead and renders a different shade.",
                    "type": "string",
                    "enum": [
                      "blue",
                      "darkBlue",
                      "orange",
                      "gray",
                      "red",
                      "green",
                      "violet"
                    ]
                  },
                  "title": {
                    "description": "column header label",
                    "type": "string"
                  },
                  "valueKey": {
                    "description": "key used to extract the value from row data",
                    "type": "string"
                  },
                  "width": {
                    "description": "antd Table column width \u2014 a pixel number, or any CSS length string. Mirrors antd ColumnType.width. Optional: omitted, antd sizes the column as it does today.",
                    "type": [
                      "integer",
                      "string"
                    ]
                  },
                  "minWidth": {
                    "description": "antd Table column minWidth in px. Mirrors antd ColumnType.minWidth. Usually the better lever under fitContent: a fixed width re-creates the overflow it was meant to fix, while a floor keeps a column readable and lets the others flex.",
                    "type": "integer"
                  }
                },
                "required": [
                  "valueKey",
                  "title"
                ],
                "additionalProperties": false
              }
            },
            "dataSource": {
              "type": "array",
              "description": "antd Table dataSource \u2014 the table rows (renamed from `data`; `data` still accepted for back-compat)",
              "items": {
                "type": "array",
                "description": "Row represented as array of cells",
                "items": {
                  "type": "object",
                  "properties": {
                    "valueKey": {
                      "description": "the key of the column this cell belongs to",
                      "type": "string"
                    },
                    "kind": {
                      "type": "string",
                      "enum": [
                        "jsonSchemaType",
                        "icon",
                        "widget",
                        "tag",
                        "bar",
                        "conditions"
                      ],
                      "description": "type of cell value. `tag` renders the stringValue as a colored antd Tag (use the cell `color`). `bar` renders a reconciliation-rail Progress gauge (stringValue = percent 0-100, `color` = bar/state color, amber target-tick at 100%). `conditions` renders `arrayValue` (an array of {type,status}) as small status pills, each coloured cyan (True) / crimson (False)."
                    },
                    "color": {
                      "type": "string",
                      "description": "per-cell color \u2014 for a `tag` cell, an antd Tag color (e.g. green / red / gold / blue); for a jsonSchemaType cell it overrides the column color. Lets each row carry its own color (e.g. status)."
                    },
                    "format": {
                      "type": "string",
                      "enum": [
                        "relative",
                        "date",
                        "datetime"
                      ],
                      "description": "optional display format for a string value \u2014 `relative` (e.g. \"14d ago\"), `date`, or `datetime`. The raw value stays in the data; only the rendering changes."
                    },
                    "resourceRefId": {
                      "type": "string",
                      "description": "used if kind = widget"
                    },
                    "type": {
                      "type": "string",
                      "enum": [
                        "string",
                        "number",
                        "integer",
                        "decimal",
                        "boolean",
                        "array",
                        "null"
                      ],
                      "description": "used if kind = jsonSchemaType"
                    },
                    "stringValue": {
                      "type": "string",
                      "description": "value if type = string"
                    },
                    "numberValue": {
                      "type": "integer",
                      "description": "value if type = number or integer"
                    },
                    "decimalValue": {
                      "type": "string",
                      "description": "value if type = number or decimal"
                    },
                    "booleanValue": {
                      "type": "boolean",
                      "description": "value if type = boolean"
                    },
                    "arrayValue": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      },
                      "description": "value if type = array"
                    }
                  },
                  "required": [
                    "valueKey",
                    "kind"
                  ],
                  "additionalProperties": false
                }
              }
            },
            "pagination": {
              "type": "object",
              "additionalProperties": false,
              "description": "antd Table pagination config (subproperties mirror antd).",
              "properties": {
                "pageSize": {
                  "type": "integer",
                  "description": "number of rows per page"
                },
                "defaultPageSize": {
                  "type": "integer",
                  "description": "default page size"
                },
                "total": {
                  "type": "integer",
                  "description": "server-side pagination: the TOTAL row count across all pages (the widget's `dataSource` holds only the current page/window). Set by the widgetDataTemplate from the full list length so the pager renders the correct number of pages. When present, the Table uses controlled server-side pagination (each page fetched on demand) instead of client-side slicing of dataSource."
                },
                "current": {
                  "type": "integer",
                  "description": "server-side pagination: the 1-based current page (controlled). Usually driven by the request `page` param, not the CR."
                },
                "hideOnSinglePage": {
                  "type": "boolean",
                  "description": "hide the pager when there is a single page"
                },
                "simple": {
                  "type": "boolean",
                  "description": "use the simple pager"
                },
                "position": {
                  "type": "array",
                  "description": "pager position(s)",
                  "items": {
                    "type": "string",
                    "enum": [
                      "topLeft",
                      "topCenter",
                      "topRight",
                      "bottomLeft",
                      "bottomCenter",
                      "bottomRight"
                    ]
                  }
                }
              }
            },
            "bordered": {
              "type": "boolean",
              "description": "antd Table bordered"
            },
            "fitContent": {
              "type": "boolean",
              "description": "Fit columns to the container instead of horizontal-scrolling: a wide cell ellipsis-truncates (full text on hover) rather than pushing a scrollbar. Ignored for virtualized (very large) tables."
            },
            "size": {
              "type": "string",
              "enum": [
                "large",
                "middle",
                "small"
              ],
              "description": "antd Table size. Defaults to 'middle' when omitted (matching Button), so an unset value has a documented, Brand-consistent density rather than antd's raw default"
            },
            "prefix": {
              "description": "it's the filters prefix to get right values",
              "type": "string"
            },
            "rowNavigateTo": {
              "description": "optional route path to navigate to on row click; `{valueKey}` placeholders are filled from that row's cells (e.g. /compositions/{ns}/{name})",
              "type": "string"
            },
            "watch": {
              "description": "live-refresh watch: involvedObject(s) this widget is tied to (see src/schemas/watch.schema.json). A matching k8s event refetches the widget.",
              "type": "array",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "apiVersion",
                  "kind"
                ],
                "properties": {
                  "apiVersion": {
                    "type": "string",
                    "description": "group/version, e.g. composition.krateo.io/v1alpha1"
                  },
                  "kind": {
                    "type": "string",
                    "description": "e.g. DemoClaim"
                  },
                  "namespace": {
                    "type": "string",
                    "description": "scope to a namespace; omit to match any"
                  },
                  "name": {
                    "type": "string",
                    "description": "a specific object; omit to match any object of this kind (\"GVR-level\")"
                  }
                }
              }
            }
          },
          "additionalProperties": false,
          "description": "The properties Table renders. Where Table wraps an antd component these are antd's own prop names, so antd's documentation for that component describes them."
        },
        "resourcesRefs": {
          "type": "object",
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "allowed": {
                    "type": "boolean",
                    "description": "Set server-side: whether the caller may perform this verb. False entries are stripped before the widget renders and are never authored by hand."
                  },
                  "apiVersion": {
                    "type": "string",
                    "description": "Group and version of the referenced resource, for example `widgets.templates.krateo.io/v1beta1`."
                  },
                  "id": {
                    "type": "string",
                    "description": "The author-chosen handle this widget uses to reference the resource, matching a resourceRefId in widgetData."
                  },
                  "name": {
                    "type": "string",
                    "description": "Name of the referenced resource."
                  },
                  "namespace": {
                    "type": "string",
                    "description": "Namespace of the referenced resource."
                  },
                  "payload": {
                    "type": "object",
                    "description": "Body sent with a mutating verb. Ignored for a GET."
                  },
                  "resource": {
                    "type": "string",
                    "description": "Plural resource name, for example `paragraphs`."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "POST",
                      "PUT",
                      "PATCH",
                      "DELETE",
                      "GET"
                    ],
                    "description": "HTTP verb used to resolve the reference. GET for a read; a mutating verb makes this a write the widget can dispatch."
                  },
                  "slice": {
                    "type": "object",
                    "properties": {
                      "offset": {
                        "type": "integer",
                        "description": "Number of items to skip before the returned page."
                      },
                      "page": {
                        "type": "integer",
                        "description": "One-based page number to return."
                      },
                      "perPage": {
                        "type": "integer",
                        "description": "Maximum items in the returned page."
                      },
                      "continue": {
                        "type": "boolean",
                        "description": "Opaque continuation token from a previous response, used to fetch the next page."
                      }
                    },
                    "required": [
                      "page",
                      "perPage"
                    ],
                    "description": "Server-side paging for this reference: the widget receives one page rather than the whole list."
                  }
                },
                "required": [
                  "id"
                ]
              },
              "description": "One referenced resource. The widget addresses it by its `id`, never by name."
            }
          },
          "required": [
            "items"
          ],
          "description": "The resources this widget references by id. Each entry is resolved server-side and its endpoint handed to the widget; entries the caller may not read are removed before the widget renders, so a denied reference reads as absence rather than as an error."
        },
        "apiRef": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Name of the RESTAction resource to call."
            },
            "namespace": {
              "type": "string",
              "description": "Namespace of the RESTAction resource."
            }
          },
          "additionalProperties": false,
          "required": [
            "name",
            "namespace"
          ],
          "description": "Binds this widget to a RESTAction whose response feeds the widgetDataTemplate and resourcesRefsTemplate expressions. Omit it for a widget whose data is entirely static."
        },
        "widgetDataTemplate": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "forPath": {
                "type": "string",
                "description": "Dot-path inside widgetData whose value this expression replaces, for example `items[0].title`."
              },
              "expression": {
                "type": "string",
                "description": "A jq expression evaluated server-side against the apiRef response. Its result replaces the value at forPath."
              }
            },
            "additionalProperties": false
          },
          "additionalProperties": false,
          "description": "Per-field overrides evaluated against the apiRef response, so one authored widget can render live cluster data. Each entry replaces one value inside widgetData."
        },
        "resourcesRefsTemplate": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "iterator": {
                "type": "string",
                "description": "A jq expression selecting the array in the apiRef response to iterate over."
              },
              "template": {
                "type": "object",
                "properties": {
                  "apiVersion": {
                    "type": "string",
                    "description": "Group and version of the referenced resource, for example `widgets.templates.krateo.io/v1beta1`."
                  },
                  "id": {
                    "type": "string",
                    "description": "The author-chosen handle this widget uses to reference the resource, matching a resourceRefId in widgetData."
                  },
                  "name": {
                    "type": "string",
                    "description": "Name of the referenced resource."
                  },
                  "namespace": {
                    "type": "string",
                    "description": "Namespace of the referenced resource."
                  },
                  "payload": {
                    "type": "object",
                    "description": "Body sent with a mutating verb. Ignored for a GET."
                  },
                  "resource": {
                    "type": "string",
                    "description": "Plural resource name, for example `paragraphs`."
                  },
                  "verb": {
                    "type": "string",
                    "enum": [
                      "POST",
                      "PUT",
                      "PATCH",
                      "DELETE",
                      "GET"
                    ],
                    "description": "HTTP verb used to resolve the reference. GET for a read; a mutating verb makes this a write the widget can dispatch."
                  }
                },
                "additionalProperties": false,
                "description": "The resourcesRefs entry emitted for each iterated element; its fields may interpolate that element."
              }
            },
            "additionalProperties": false
          },
          "description": "Generates resourcesRefs entries from the apiRef response \u2014 one per element the iterator yields \u2014 so a widget can reference a list whose length it does not know when authored."
        }
      },
      "required": [
        "widgetData"
      ],
      "additionalProperties": false,
      "description": "The widget's contract: its own data, an optional binding to a RESTAction, the resources it references, and any templated overrides."
    }
  },
  "required": [
    "kind",
    "spec",
    "version"
  ]
}
